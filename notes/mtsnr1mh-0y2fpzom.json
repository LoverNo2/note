{
  "id": "mtsnr1mh-0y2fpzom",
  "title": "Godot",
  "content": "<h1>非运行时</h1><h5>SceneState</h5><p>Godot引擎从始至终只处理一样东西就是Node,Node本质上是C++对象,在引擎界面中任何操作都是在调用API处理Node</p><pre><code>                pack()                  save()序列化\n   Node 树  ─────────────►  SceneState  ─────────────►  .tscn / .scn\n\n                instantiate()                 load()\n   Node 树  ◄─────────────  SceneState  ◄─────────────  .tscn / .scn</code></pre><p>PackedScene是Resource壳,SceneState是它持有的纯数据</p><pre><code>class PackedScene {\n    state = new SceneState()\n\n    // 调用 SceneState::pack\n    pack(scene) { ... }\n\n    // 调用 SceneState::instantiate \n    instantiate() { ... }\n}</code></pre><p>SceneState::pack(node)做的事,是递归遍历节点树,把每个节点拍扁成一行记录,归档成SceneState的那套下标池数据</p><pre><code>class SceneState {\n\n    // 字符串池：类型名、属性名、组名、信号名、方法名都只存下标\n    names = []\n\n    // 变体池：属性值、子场景 PackedScene 引用\n    variants = []\n\n    // NodeData[],下标即节点序号,[0] 是根\n    nodes = []\n\n    // ConnectionData[]\n    connections = []\n\n    // NodeData { parent, owner, type, name, instance, index, properties, groups }\n    // ConnectionData { from, to, signal, method, flags, unbinds, binds }\n\n    pack(scene) { ... }\n    instantiate() { ... }\n}\n\n// SceneState::NodeData —— 一个节点的存档；字段全是下标,指向 SceneState 的各池\nconst node_data = {\n\n    // nodes 下标；\n    parent: 0,\n\n    // nodes 下标\n    owner: 0,\n\n    // names 下标；TYPE_INSTANTIATED 表示这个节点来自子场景实例\n    type: 0,\n\n    // names 下标\n    name: 0,\n\n    // 子场景的 PackedScene 在 variants 里的下标\n    instance: 0,\n\n    // 在父节点 children 中的位置\n    index: 0,\n\n    // [{ name: names 下标, value: variants 下标 }]\n    properties: [],\n\n    // names 下标列表\n    groups: [],\n}\n\n// SceneState::ConnectionData —— 一条信号连接的存档\nconst connection_data = {\n\n    // nodes 下标\n    from: 0,\n\n    // nodes 下标\n    to: 0,\n\n    // names 下标\n    signal: 0,\n\n    // names 下标\n    method: 0,\n\n    // ConnectFlags\n    flags: 0,\n    unbinds: 0,\n\n    // variants 下标列表\n    binds: [],\n}\n</code></pre><h5>instantiate</h5><pre><code>// 1 PackedScene::instantiate 只是个薄壳,真正的活在 SceneState::instantiate\nfunction instantiate() {\n    // tree存放创建好的节点\n    const tree = []\n\n    for (let i = 0; i &lt; nodes.size(); i++) {\n        const n = nodes[i]\n\n        // 获取父节点\n        const parent = i &gt; 0 ? node_from_id(n.parent) : null\n\n        // 创建点\n        let node = ClassDB::instantiate(names[n.type])\n\n        // 添加属性\n        for (const p of n.properties) {\n            node.set(names[p.name], variants[p.value])\n        }\n        for (const g of n.groups) {\n            node.add_to_group(names[g], true)\n            // → 编辑器下 node.emit_signal(\"editor_state_changed\")\n        }\n\n        if (i == 0) {\n            // 根节点命名\n            node._set_name_nocheck(names[n.name])\n        } else {\n            parent._add_child_nocheck(node, names[n.name])\n\n            // 把 node 移动到 parent 的子节点列表中索引为 n.index 的位置\n            parent.move_child(node, n.index)\n            // → parent.notification(NOTIFICATION_CHILD_ORDER_CHANGED)\n            // → parent.emit_signal(\"child_order_changed\")\n        }\n\n        node._set_owner_nocheck(node_from_id(n.owner))\n\n        tree[i] = node\n    }\n\n    return tree[0]\n}</code></pre><h5>add_child</h5><pre><code>function _add_child_nocheck(child, name) {\n    // 建立父子关系\n    child.data.name = name\n    data.children.insert(name, child)\n    child.data.parent = this\n\n    // 通知：已经被挂到父节点下\n    child.notification(NOTIFICATION_PARENTED)\n\n    // 如果当前节点（父节点）已经在 SceneTree 中（data.tree 非空）,那么新加入的子节点也应该立即进入场景树\n    if (data.tree)\n        child._set_tree(data.tree)\n    // → child 进树（父已在 SceneTree 时）：ENTER_TREE → POST_ENTER_TREE → READY\n\n    notification(NOTIFICATION_CHILD_ORDER_CHANGED)\n    emit_signal(\"child_order_changed\")\n}</code></pre><h5>set_tree</h5><pre><code>function _set_tree(tree) {\n    const old_tree = data.tree\n\n    // 原来在树里：让旧树退出\n    if (old_tree) {\n        _propagate_exit_tree()\n        // → emit_signal(\"tree_exiting\")\n        // → node：NOTIFICATION_EXIT_TREE（反向）\n        // → parent：emit_signal(\"child_exiting_tree\")\n    }\n\n    data.tree = tree\n\n    // 进新树\n    if (data.tree) {\n        _propagate_enter_tree()\n        // → node：NOTIFICATION_ENTER_TREE → emit_signal(\"tree_entered\")\n        // → tree：node_added(node)、parent：emit_signal(\"child_entered_tree\")\n        // → 子节点各自递归走一遍\n\n        // 没有父节点（根）或父节点已 ready,才继续走 ready 链\n        if (!data.parent || data.parent.data.ready_notified)\n            _propagate_ready()\n        // → node：NOTIFICATION_POST_ENTER_TREE,然后 NOTIFICATION_READY + emit_signal(\"ready\")\n    }\n\n    // 通知新旧树\n    if (old_tree) {\n        old_tree.tree_changed()\n        // → emit_signal(\"tree_changed\")\n    }\n    if (tree) {\n        tree.tree_changed()\n        // → emit_signal(\"tree_changed\")\n    }\n}</code></pre><h5>propagate_enter_tree</h5><pre><code>function _propagate_enter_tree() {\n    // 从父节点继承所属scenetree\n    data.tree = data.parent.data.tree\n\n    // 记住本节点落在哪个 Viewport（自己不是就继承父节点的）\n    data.viewport = this instanceof Viewport ? this : data.parent.data.viewport\n\n    // 加进它所属的各个 group（并记下 group 句柄,退出时用）\n    for (const name of data.grouped.keySet())\n        data.tree.add_to_group(name, this)\n\n    notification(NOTIFICATION_ENTER_TREE)\n\n    // 脚本的 _enter_tree() 回调\n    _enter_tree()\n\n    emit_signal(\"tree_entered\")\n\n    // 树登记新成员\n    data.tree.node_added(this)\n    // → emit_signal(\"node_added\")\n\n    // 父节点广播\n    if (data.parent)\n        data.parent.emit_signalp(\"child_entered_tree\", this)\n\n    // 子节点递归\n    for (const child of data.children)\n        child._propagate_enter_tree()\n}</code></pre><h5>propagate_exit_tree</h5><pre><code>function _propagate_exit_tree() {\n    // 子节点先退,顺序与进树相反\n    for (const child of data.children.reverse())\n        child._propagate_exit_tree()\n\n    // 脚本的 _exit_tree() 回调\n    _exit_tree()\n\n    emit_signal(\"tree_exiting\")\n    notification(NOTIFICATION_EXIT_TREE, true)\n\n    // scenetree 注销本节点\n    data.tree.node_removed(this)\n    // → emit_signal(\"node_removed\")\n\n    // 父节点广播\n    if (data.parent)\n        data.parent.emit_signalp(\"child_exiting_tree\", this)\n\n    // 退出各个 group\n    for (const [name, entry] of data.grouped) {\n        data.tree.remove_from_group(name, this)\n        entry.group = null\n    }\n\n    // 通知树：成员变了\n    data.tree.tree_changed()\n    // → emit_signal(\"tree_changed\")\n\n    // 清空与树的关联\n    data.viewport = null\n    data.ready_notified = false\n    data.tree = null\n    data.depth = -1\n}</code></pre><h5>propagate_ready</h5><pre><code>function _propagate_ready() {\n    data.ready_notified = true\n\n    // 子节点先 ready,再轮到自己\n    for (const child of data.children)\n        child._propagate_ready()\n\n    notification(NOTIFICATION_POST_ENTER_TREE)\n\n    // 只有还没 ready 过的节点才真正发 READY（request_ready() 会把它置回未 ready）\n    if (data.ready_first) {\n        data.ready_first = false\n        notification(NOTIFICATION_READY)\n        emit_signal(\"ready\")\n    }\n}</code></pre><h5>notification</h5><pre><code>function notification(what, reversed = false) {\n    if (reversed) {\n        _notification_backward(what)\n    } else {\n        _notification_forward(what)\n    }\n}</code></pre><h5>_notification_forward</h5><pre><code>function _notification_forward(what) {\n    _notification_forwardv(what)\n    script_instance.notification(what, false)\n}\n\nfunction _notification_backward(what) {\n    script_instance.notification(what, true)\n    _notification_backwardv(what)\n}</code></pre><h5>_notification_forwardv</h5><pre><code>function _notification_forwardv(what) {\n    super._notification_forwardv(what)\n    this._notification(what)\n}\n\nfunction _notification_backwardv(what) {\n    this._notification(what)\n    super._notification_backwardv(what)\n}</code></pre><h5>_notification</h5><pre><code>// Object\nfunction _notification(what) {}\n\n// Node：scene/main/node.cpp:68\nfunction _notification(what) {\n    switch (what) {\n        case NOTIFICATION_PROCESS:\n            // 脚本的 _process(delta) 回调\n            _process(get_process_delta_time())\n            break\n\n        case NOTIFICATION_PHYSICS_PROCESS:\n            // 脚本的 _physics_process(delta) 回调\n            _physics_process(get_physics_process_delta_time())\n            break\n\n        case NOTIFICATION_ENTER_TREE:\n            // 选定 process 归属：继承父节点的,或自己当家\n            data.process_owner = data.parent ? data.parent.data.process_owner : this\n\n            // 定下 process 分组,并把节点挂进 process 线程组\n            data.process_group = data.tree.default_process_group\n            _add_process_group()\n            if (_is_any_processing())\n                _add_to_process_thread_group()\n\n            // 物理插值开关往下传播\n            _propagate_physics_interpolated(interpolate)\n\n            // 按输入标志把自己登记进 _vp_* 特殊 group（输入分发的索引）\n            if (data.input)\n                add_to_group(\"_vp_input\" + get_viewport().get_instance_id())\n            if (data.shortcut_input)\n                add_to_group(\"_vp_shortcut_input\" + get_viewport().get_instance_id())\n            if (data.unhandled_input)\n                add_to_group(\"_vp_unhandled_input\" + get_viewport().get_instance_id())\n            if (data.unhandled_key_input)\n                add_to_group(\"_vp_unhandled_key_input\" + get_viewport().get_instance_id())\n\n            data.tree.nodes_in_tree_count++\n            break\n\n        case NOTIFICATION_POST_ENTER_TREE:\n            // 进树完成后自己再补一条翻译变更通知\n            notification(NOTIFICATION_TRANSLATION_CHANGED)\n            break\n\n        case NOTIFICATION_EXIT_TREE:\n            // ENTER_TREE 那一套登记逐项撤销,顺序与登记时相反\n            AccessibilityServer.free_element(data.accessibility_element)\n            remove_from_group(\"_vp_input\" + get_viewport().get_instance_id())\n            remove_from_group(\"_vp_shortcut_input\" + get_viewport().get_instance_id())\n            remove_from_group(\"_vp_unhandled_input\" + get_viewport().get_instance_id())\n            remove_from_group(\"_vp_unhandled_key_input\" + get_viewport().get_instance_id())\n            _remove_from_process_thread_group()\n            if (data.process_thread_group_owner == this)\n                _remove_process_group()\n            data.process_owner = null\n            data.path_cache = null\n            data.tree.nodes_in_tree_count--\n            break\n\n        case NOTIFICATION_PAUSED:\n        case NOTIFICATION_SUSPENDED:\n            // 暂停 / 挂起时复位物理插值\n            reset_physics_interpolation()\n            break\n\n        case NOTIFICATION_PATH_RENAMED:\n            // 路径变了,旧的路径缓存作废\n            data.path_cache = null\n            break\n\n        case NOTIFICATION_READY:\n            // 脚本覆写了哪个虚拟函数,就打开对应的 process\n            if (GDVIRTUAL_IS_OVERRIDDEN(_input))\n                set_process_input(true)\n            if (GDVIRTUAL_IS_OVERRIDDEN(_shortcut_input))\n                set_process_shortcut_input(true)\n            if (GDVIRTUAL_IS_OVERRIDDEN(_unhandled_input))\n                set_process_unhandled_input(true)\n            if (GDVIRTUAL_IS_OVERRIDDEN(_unhandled_key_input))\n                set_process_unhandled_key_input(true)\n            if (GDVIRTUAL_IS_OVERRIDDEN(_process))\n                set_process(true)\n            if (GDVIRTUAL_IS_OVERRIDDEN(_physics_process))\n                set_physics_process(true)\n\n            // 脚本的 _ready() 回调\n            _ready()\n            break\n\n        case NOTIFICATION_PREDELETE:\n            // 与 owner 解绑、从父节点摘掉、再逐个删掉子节点\n            data.owner._clean_up_owner()\n            for (const owned of data.owned)\n                owned._clean_up_owner()\n            data.parent.remove_child(this)\n            while (data.children.size())\n                memdelete(data.children.last())\n            break\n\n        case NOTIFICATION_TRANSLATION_CHANGED:\n            data.is_auto_translate_dirty = true\n            break\n    }\n}</code></pre><h1>运行时主循环</h1><h5>详解</h5><p>Main::iteration()方法由平台层每帧调用一次,其内部将一帧拆分为3段：若干次固定步长物理帧 → 一次可变步长逻辑帧 → 一次渲染提交,最后睡眠等待下一帧</p><pre><code>const advance = main_timer_sync.advance(physics_step, physics_ticks_per_second)\nlet process_step = advance.process_step</code></pre><p>advance的类型是MainFrameTime,只有3个字段,没有任何嵌套结构</p><table><colgroup><col style=\"width: 287px;\"><col style=\"width: 1003px;\"></colgroup><tbody><tr><th class=\"\">字段<br></th><th class=\"\">含义<br></th></tr><tr><td class=\"\">process_step<br></td><td>\t这一帧逻辑帧（process()）应推进的秒数,未乘 time_scale<br></td></tr><tr><td class=\"\">physics_steps<br></td><td class=\"\">这一帧要迭代物理引擎的次数（即物理帧循环的次数）<br></td></tr><tr><td class=\"\">interpolation_fraction<br></td><td class=\"\">当前这一帧「卡在物理 tick 内的百分之几」,即物理插值进度,范围 [0, 1)<br></td></tr></tbody></table><pre><code>if (fixed_fps == -1 &amp;&amp; advance.physics_steps &gt; max_physics_steps) {\n    process_step -= (advance.physics_steps - max_physics_steps) * physics_step\n    advance.physics_steps = max_physics_steps\n}</code></pre><p>max_physics_steps默认为8,某帧卡顿1秒时,advance会算出几十个物理步,若照跑会「追帧雪崩」.这里把物理步数截到上限,同时从 process_step 里扣掉被丢弃的那部分物理时间——否则物理只消费了 8 步、逻辑帧却仍按整个墙钟步长推进,会出现物理/逻辑时间账不一致.扣掉之后逻辑帧步长与物理实际消费的时间对齐,scaled_step 也随之变小</p><pre><code>let exit = false</code></pre><p>标记“该退出”,函数后面仍会走完绘制和收尾,最后才return</p><pre><code>const main_loop = os.get_main_loop()</code></pre><p>对应源码里反复出现的 OS::get_singleton()-&gt;get_main_loop()（如 main.cpp:4986/5001/5047）.主循环对象通常就是 SceneTree,但 --script 等场景下可以是自定义 MainLoop,所以每帧现取而不是缓存</p><h5>完整代码</h5><pre><code>function iteration() {\n    // 上锁\n    iterating++\n\n    // physics_ticks_per_second 来自项目设置,默认60,也就是默认每秒60个物理帧\n    const physics_step = 1.0 / physics_ticks_per_second\n    const time_scale = engine.get_effective_time_scale()\n\n    const advance = main_timer_sync.advance(physics_step, physics_ticks_per_second)\n    let process_step = advance.process_step\n\n    // max_physics_steps 默认为 8\n    if (advance.physics_steps &gt; max_physics_steps) {\n        process_step -= (advance.physics_steps - max_physics_steps) * physics_step\n        advance.physics_steps = max_physics_steps\n    }\n\n    let exit = false\n\n    const main_loop = os.get_main_loop()\n\n    for (let iters = 0; iters &lt; advance.physics_steps; ++iters) {\n        main_loop.physics_process(physics_step * time_scale)\n    }\n\n    main_loop.process(process_step * time_scale)\n\n    rendering_server.draw()\n\n    // 解锁\n    iterating--\n\n    return exit\n}</code></pre><h5>_process</h5><p>_process()本身不遍历节点、也不发通知——它是一个\"调度器\"。真正给节点发 NOTIFICATION_PROCESS / NOTIFICATION_PHYSICS_PROCESS 的是 _process_group()。_process() 的职责只有一句：决定本帧哪些\"处理组(ProcessGroup)\"要跑、以什么顺序跑、哪些可以并行跑。</p><pre><code>// ProcessGroup(scene_tree.h:100)——一组共用同一\"处理配置\"的节点\n{\n\n    // 这个组的延迟调用队列(deferred)\n    call_queue,\n\n    // 普通节点\n    nodes,\n\n    // 物理节点\n    physics_nodes,\n\n    // 被标删,等下一次 dirty 重建时清掉\n    removed,\n\n    // 决定这组怎么排/怎么跑的\"所有者\"节点(可为 null)\n    owner,\n\n    // 上次被判定\"本帧有效\"的 pass 号\n    last_pass,\n}</code></pre><p>节点的处理配置来自 owner.data.process_thread_group_order 和 process_thread_group（set_process_thread_group_order() / set_process_thread_group() 设的）。owner == null 的那个组就是 default_process_group。</p><p>process_groups 是这些组指针的数组；process_groups_dirty 表示数组该重建了。</p><p>process_last_pass 是一个自增的\"帧号\"，用来给组打\"本帧有效\"标记。</p><pre><code>// from: scene/main/scene_tree.cpp:1244-1360\nfunction _process(p_physics) {\n    // ── 阶段 1：整理组列表（只在 dirty 时做） ───────────────────────\n    if (process_groups_dirty) {\n        // 摘掉被标记 removed 的组：用「最后一个组补位」的方式原地删除\n        for (i = 0; i &lt; pg_count; i++)\n            if (pg_ptr[i].removed) { pg_ptr[i] = pg_ptr[--pg_count]; i-- }\n        process_groups.resize(pg_count)\n\n        // 排序：主键 owner.process_thread_group_order,同序时「子线程组」排在前面\n        process_groups.sort_custom&lt;ProcessGroupSort&gt;()\n        process_groups_dirty = false\n    }\n\n    // 处理期间新加的组会追加到末尾,本帧忽略；组不会在处理中被移除\n    const group_count = process_groups.size()\n    if (group_count == 0)\n        return\n\n    process_last_pass++\n    nodes_removed_on_group_call_lock++\n\n    // ── 阶段 2：筛出本帧「有效」的组,打上 last_pass 标记 ──────────────\n\n    // i == group_count 是哨兵,用于冲刷最后一段\n    for (i = 0; i &lt;= group_count; i++) {\n        // 段落边界：order 或 threaded 变了\n        if (i == group_count || current_order != order || current_threaded != threaded) {\n            if (process_count &gt; 0) {\n                using_threads = 该段首个 owner 是 PROCESS_THREAD_GROUP_SUB_THREAD &amp;&amp; !node_threading_disabled\n                for (j = from; j &lt; i; j++)\n                    if (process_groups[j].last_pass == process_last_pass) {\n                        if (using_threads)\n                            local_process_group_cache.push_back(process_groups[j])\n\n                        // ← 顺序执行\n                        else               _process_group(process_groups[j], p_physics)\n                    }\n\n                // ← 线程组整段丢给 WorkerThreadPool,并等它跑完\n                if (using_threads)\n                    wait_for_group_task_completion(add_template_group_task(_process_groups_thread, p_physics, ...))\n            }\n            if (i == group_count)\n                break\n            from = i; current_threaded = threaded; current_order = order\n        }\n\n        if (process_groups[i].removed)\n            continue\n\n        // 这个组本帧有话可说吗？\n        if (p_physics) {\n            process_valid = !pg.physics_nodes.is_empty()\n                || ((pg == &amp;default_process_group || owner.process_thread_messages.has_flag(FLAG_PROCESS_THREAD_MESSAGES_PHYSICS))\n                    &amp;&amp; pg.call_queue.has_messages())\n        } else {\n            process_valid = !pg.nodes.is_empty()\n                || ((pg == &amp;default_process_group || owner.process_thread_messages.has_flag(FLAG_PROCESS_THREAD_MESSAGES))\n                    &amp;&amp; pg.call_queue.has_messages())\n        }\n        if (process_valid) { pg.last_pass = process_last_pass; process_count++ }\n    }\n\n    nodes_removed_on_group_call_lock--\n    if (nodes_removed_on_group_call_lock == 0)\n        nodes_removed_on_group_call.clear()\n}</code></pre><pre><code>// from: scene/main/scene_tree.cpp:1177-1236\n// 注释原话：这个函数必须保证「任何节点被移除都不影响继续处理」\nfunction _process_group(p_group, p_physics) {\n\n    // 处理前先执行积压的跨线程消息\n    p_group.call_queue.flush()\n\n    const nodes = p_physics ? p_group.physics_nodes : p_group.nodes\n    if (nodes.is_empty())\n        return\n\n    // 按优先级排序（dirty 才排）\n\n    // 物理优先级；逻辑帧用 ComparatorWithPriority\n    nodes.sort_custom&lt;ComparatorWithPhysicsPriority&gt;()\n    // 比较规则：先比 process_priority/physics_process_priority（大的先）,相同时比 is_greater_than()（树中靠后的先,注释：child(1) &gt; child(0) &gt; parent）\n\n    // 复制一份,处理期间增删不会破坏遍历\n    const nodes_copy = nodes\n    for (n of nodes_copy) {\n        if (nodes_removed_on_group_call.has(n))\n\n            // 调用组期间被删掉的节点跳过\n            continue\n        if (!n.can_process() || !n.is_inside_tree())\n\n            // process_mode 不允许 / 不在树里\n            continue\n\n        if (p_physics) {\n            if (n.is_physics_processing_internal())\n                n.notification(NOTIFICATION_INTERNAL_PHYSICS_PROCESS)\n            if (n.is_physics_processing())\n\n                // → Node::_notification 里 GDVIRTUAL_CALL(_physics_process, delta)\n                n.notification(NOTIFICATION_PHYSICS_PROCESS)\n        } else {\n            if (n.is_processing_internal())\n                n.notification(NOTIFICATION_INTERNAL_PROCESS)\n            if (n.is_processing())\n\n                // → 脚本 _process(delta)\n                n.notification(NOTIFICATION_PROCESS)\n        }\n    }\n\n    // 处理后再执行一次（供处理中产生的延迟调用）\n    p_group.call_queue.flush()\n}</code></pre>",
  "createdAt": "2026-09-08T12:40:43.769Z",
  "updatedAt": "2026-09-23T07:50:48.121Z"
}