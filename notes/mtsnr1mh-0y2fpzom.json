{
  "id": "mtsnr1mh-0y2fpzom",
  "title": "Godot",
  "content": "<h1>调用链</h1><pre><code>// 标准入口\nfunction main() {                                    // 标准入口\n    setup();                                         // 阶段1：解析命令行参数、初始化 Core 层、加载项目配置\n    setup2();                                        // 阶段2：初始化所有 Servers（渲染、物理、音频、导航等）、创建显示窗口\n    start();                                         // 阶段3：创建 SceneTree（默认 MainLoop）、加载主场景/当前场景\n\n    // 进入主循环（实际为 OS 层驱动） \n    mainLoop.initialize();                           // 初始化主循环\n    while (true) {\n        DisplayServer.processEvents();               // 处理系统事件\n        iteration();                                 // 引擎单帧迭代（输入→逻辑→物理→渲染）\n    }\n    mainLoop.finalize();                             // 结束主循环\n\n    cleanup();                                       // 退出清理\n}</code></pre><h1>start</h1><p>在start中，引擎会创建一个SceneTree对象（它是MainLoop的默认子类实现）。SceneTree 内部自动创建了一个Viewport节点root，它是场景根节点的父节点，也就是整个游戏的最高层节点。</p><h4>加载资源</h4><p>引擎首先读取指定的主场景文件，其中记录了场景的<b>节点</b><b>、</b><b>节点的层级关系</b><b>、</b><b>节点的属性值以及资源的引用</b>。引擎将这份蓝图解析并缓存为一个PackedScene对象。</p><h4>实例化主场景</h4><pre><code>function main() {\n    setup()                                         // 阶段1：解析命令行参数、初始化 Core 层、加载项目配置\n\n    start()                                         // 阶段3：创建 SceneTree、加载主场景\n        const mainScene = PackedScene.instantiate() // 实例化主场景\n        root.addChild(mainScene)                    // 添加到场景树 ← 触发入树流程（关键转折点）\n}</code></pre><p>引擎根据蓝图里记录的层级，从根节点开始，递归地（深度优先）在内存中new出每一个节点及其子节点。引擎为每个新节点设置好蓝图里写的所有属性，并为节点挂载脚本。在这一步结束时，完整的节点树已经在内存中创建完毕。</p><p>主场景树实例化之后，get_tree().root.add_child(主场景实例)将内存中的节点树挂载到SceneTree。</p><p></p><p>实例化和入树用的是同一个add_child()，实例化阶段因为节点不在树中所以入树逻辑不触发，只建立了父子关系；挂到场景树时因为root在树中，入树逻辑触发，一次调用就递归完成了整棵子树的入树。</p><h5>instantiate</h5><pre><code>// scene/resources/packed_scene.js — SceneState.instantiate()（简化）\nfunction instantiate(genEditState) {\n    // ① 创建所有节点对象\n    const nodes = []\n    for (let i = 0; i &lt; nodeCount; i++) {\n        const n = ClassDB.instantiate(nodeTypes[i])\n        n.setName(nodeNames[i])\n        // 设置属性...\n        nodes.push(n)\n    }\n\n    // ② 建立父子关系\n    for (let i = 1; i &lt; nodeCount; i++) {  // 从 1 开始，0 是根节点\n        const parentIdx = nodeParentIndices[i]   // 从 .tscn 中读取的父节点索引\n        nodes[parentIdx].addChild(nodes[i])      // ★ 建立父子关系 ★\n    }\n\n    // ③ 返回根节点（不在场景树中）\n    return nodes[0]\n}</code></pre><h5>add_child</h5><pre><code>// scene/main/node.js\nfunction addChild(child, forceReadableName = false, internal = InternalMode.MODE_DISABLED) {\n    // ① 合法性校验\n    if (!child) throw new Error(\"child is null\")\n    if (child === this) throw new Error(\"child cannot be self\")\n    if (child.data.parent !== null) throw new Error(\"child already has a parent\")\n\n    // ② 建立父子关系（仅修改数据结构，不触发通知）\n    child.data.parent = this\n    this.children.push(child)\n    child.data.index = this.children.length - 1\n\n    // ③ 设置名称（去重）\n    if (forceReadableName) {\n        validateChildName(child)\n    }\n\n    // ④ 关键：如果当前节点已在场景树中，立即将子节点也加入\n    if (this.isInsideTree()) {\n        propagateEnterTree(child)   // ← 递归入树\n    }\n}</code></pre><h5>_propagate_enter_tree</h5><pre><code>// scene/main/node.js\nfunction propagateEnterTree(root) {\n    // 对 root 及其所有后代，执行深度优先的前序遍历\n\n    // 1 设置 SceneTree 指针\n    root.data.tree = getTree()\n\n    // 2 设置 Viewport 指针\n    root.data.viewport = getViewport()\n\n    // 3 发送 NOTIFICATION_ENTER_TREE 通知\n    root.notification(NOTIFICATION_ENTER_TREE)\n\n    // 4 发出 tree_entered 信号\n    root.emitSignal(\"tree_entered\")\n\n    // 5 递归处理子节点（前序：先处理当前节点，再处理子节点）\n    for (let i = 0; i &lt; root.children.length; i++) {\n        propagateEnterTree(root.children[i])\n    }\n\n    // 6 所有子节点都入树后，发送 NOTIFICATION_READY，后序\n    root.notification(NOTIFICATION_READY)\n\n    // 7 发出 ready 信号\n    root.emitSignal(\"ready\")\n}</code></pre><h1>生命周期</h1><p>“生命周期”不是一个实体、不是一个对象、不是一段可以看到的代码。它是引擎在几个关键时间点主动回调节点代码的<b>约定</b>。引擎自己维护着场景树和主循环，它知道“入树”、“离树”、“处理输入”、“推进物理”、“更新逻辑”的时间点，在这些时间点上，引擎遍历相关节点，调用它们对应的函数，这些函数就是“生命周期回调”，这些时间点串联起来就是“生命周期”。</p><table><tbody><tr><th style=\"text-align: center\">专用函数<br></th><th style=\"text-align: center\">对应的通知常量<br></th><th style=\"text-align: center\">触发时机<br></th></tr><tr><td>_ready()<br></td><td>NOTIFICATION_READY<br></td><td>节点及其所有子节点进入场景树后<br></td></tr><tr><td>_enter_tree()<br></td><td>NOTIFICATION_ENTER_TREE<br></td><td>节点进入场景树时<br></td></tr><tr><td>_exit_tree()<br></td><td>NOTIFICATION_EXIT_TREE<br></td><td>节点离开场景树时<br></td></tr><tr><td>_process(delta)<br></td><td>NOTIFICATION_PROCESS<br></td><td>每一帧的处理过程中<br></td></tr><tr><td>_physics_process(delta)<br></td><td>NOTIFICATION_PHYSICS_PROCESS<br></td><td>每一物理帧的处理过程中<br></td></tr><tr><td>_draw()<br></td><td>NOTIFICATION_DRAW<br></td><td>CanvasItem 需要重绘时<br></td></tr></tbody></table><h2>通知系统</h2><pre><code>// ------ Object 基类 ------\nclass Object {\n    notification(what) { \n        // 虚函数分派\n        // this 指向实际对象（如 Sprite2D），所以 this._notification\n        this._notification(what);\n    }\n\n    // 基类默认实现（空）\n    _notification(what) {\n        \n    }\n}\n\n// ------ Node 类 ------\nclass Node extends Object {\n    _notification(what) {\n        if (what === NOTIFICATION_ENTER_TREE) {\n            // 引擎内部操作\n            console.log(`注册到调度列表`);\n            console.log(`发射 tree_entered 信号`);\n\n            // 调用脚本层的 _notification(what)\n            if (this._notification) {\n                this._notification(what)\n            }\n\n            // 调用脚本层的 _enter_tree()\n            if (this._enter_tree) {\n                this._enter_tree()\n            }\n        }\n\n        if (what === NOTIFICATION_READY) {\n            if (this._ready) {\n                // 调用脚本层 _ready()\n                this._ready()\n            }\n        }\n    }\n}\n\n// ------ CanvasItem 类 ------\nclass CanvasItem extends Node {\n    _notification(what) {\n        if (what === NOTIFICATION_ENTER_TREE) {\n            console.log(`注册到渲染服务器`);\n        }\n\n        if (what === NOTIFICATION_VISIBILITY_CHANGED) {\n            console.log(`可见性改变，标记需要重绘`);\n        }\n\n        // call_default\n        super._notification(what); \n    }\n}\n\n// ------ Node2D 类 ------\nclass Node2D extends CanvasItem {\n    _notification(what) {\n        // Node2D 没有自己的特殊逻辑\n\n        // call_default\n        super._notification(what)\n    }\n}\n\n// ------ Sprite2D 类 ------\nclass Sprite2D extends Node2D {\n    _notification(what) {\n        // Sprite2D 没有自己的特殊逻辑\n\n        // call_default\n        super._notification(what)\n    }\n}\n\nconst sprite = new Sprite2D();\n\n// 脚本层绑定\nsprite._notification = function(what) {\n    console.log(`执行_notification脚本`);\n};\n\nsprite._enter_tree = function() {\n    console.log(`执行_enter_tree脚本`);\n};\n\nsprite._ready = function() {\n    console.log(`执行_ready脚本`);\n};\n\n// 模拟引擎调用 notification\nsprite.notification(NOTIFICATION_ENTER_TREE);\nsprite.notification(NOTIFICATION_READY);</code></pre><h5>为什么需要call_default</h5><p>对于一个Sprite2D节点，继承链是：Object → Node → CanvasItem → Node2D → Sprite2D，其中</p><ul><li>Node:注册到调度列表、发tree_entered 信号</li><li>CanvasItem:注册到渲染服务器</li><li>Node2D:无</li><li>Sprite2D:无</li></ul><p>继承链中不同类的_notification()职责个不相同并且必不可少，故而需要call_default逐层调用。</p><h1>主循环</h1><pre><code>void OS_Windows::run() {\n    if (!main_loop) return;\n\n    main_loop-&gt;initialize();   // ① 调用 MainLoop::init()，触发 SceneTree 初始化\n\n    while (true) {\n        DisplayServer::get_singleton()-&gt;process_events();  // ② 处理窗口/输入事件\n        if (Main::iteration()) {                           // ③ 引擎每帧迭代\n            break;\n        }\n    }\n\n    main_loop-&gt;finalize();     // ④ 退出时调用 MainLoop::finish()\n}</code></pre><h2>帧迭代</h2><p><code>Main.iteration</code>&nbsp;是每帧执行的核心函数，它内部会调用&nbsp;<code>MainLoop</code>&nbsp;的&nbsp;<code>iteration()</code>&nbsp;和&nbsp;<code>idle()</code>&nbsp;两个方法</p><pre><code>bool Main::iteration() {\n    // ... 帧率计算、时间同步 ...\n\n    // 物理更新（固定频率，默认 60Hz）\n    // 当物理步进累积到阈值时，调用：\n    main_loop-&gt;iteration(frame_slice * time_scale);\n    //   ↑ 内部触发 SceneTree::_physics_process() → 各节点 _physics_process(delta)\n\n    // 逻辑更新（每帧执行，频率随渲染帧率变化）\n    main_loop-&gt;idle(delta);\n    //   ↑ 内部触发 SceneTree::_process() → 各节点 _process(delta)\n\n    RenderingServer::draw() \n    // ... 渲染、音频处理 ...\n}</code></pre><h3>帧率控制</h3><pre><code>bool Main::iteration() {\n    uint64_t ticks = OS::get_singleton()-&gt;get_ticks_usec();  // 获取当前系统时间戳（微秒）\n    uint64_t ticks_elapsed = ticks - last_ticks;  // 计算上一帧到当前帧经过的时间\n    \n    last_ticks = ticks;  // 更新时间戳\n    \n    double step = (double)ticks_elapsed / 1000000.0;  // 将微秒转换为秒（浮点数）\n    float frame_slice = 1.0 / OS::get_singleton()-&gt;get_iterations_per_second();  // 物理步长时间（默认 1/60 秒）</code></pre><p><code>get_iterations_per_second()</code>&nbsp;返回每秒物理迭代次数，即项目设置中的&nbsp;<code>Physics Ticks per Second</code>（默认 60）</p><pre><code>    if (step &gt; frame_slice * 8)\n        step = frame_slice * 8;  // 限制最大步长为 8 个物理帧\n    time_accum += step;</code></pre><p>当某一帧耗时过长，引擎会限制最大追赶步数为&nbsp;8 步，防止物理模拟陷入\"死亡螺旋\"——即越追越慢、永远追不上的恶性循环</p><h3>物理帧</h3><pre><code>    float time_scale = Engine::get_singleton()-&gt;get_time_scale();\n    bool exit = false;\n    int iters = 0;\n    \n    while (time_accum &gt; frame_slice) {\n        uint64_t fixed_begin = OS::get_singleton()-&gt;get_ticks_usec();\n        \n        // ① 同步物理服务器状态\n        PhysicsServer3D::get_singleton()-&gt;sync();\n        PhysicsServer3D::get_singleton()-&gt;flush_queries();\n        PhysicsServer2D::get_singleton()-&gt;sync();\n        PhysicsServer2D::get_singleton()-&gt;flush_queries();\n        \n        // ② 调用 MainLoop::iteration() → 触发 _physics_process(delta)\n        if (OS::get_singleton()-&gt;get_main_loop()-&gt;iteration(frame_slice * time_scale)) {\n            exit = true;\n            break;\n        }\n        \n        // ③ 刷新消息队列\n        message_queue-&gt;flush();\n        \n        // ④ 执行物理步进\n        PhysicsServer3D::get_singleton()-&gt;step(frame_slice * time_scale);\n        PhysicsServer2D::get_singleton()-&gt;step(frame_slice * time_scale);\n        \n        // ⑤ 减少累加器，消费\n        time_accum -= frame_slice;\n        message_queue-&gt;flush();\n        \n        // ⑥ 记录物理帧耗时\n        fixed_process_max = MAX(OS::get_singleton()-&gt;get_ticks_usec() - fixed_begin, fixed_process_max);\n        iters++;\n    }</code></pre><p></p><p></p><h4>参数</h4><p></p><p></p><p></p><pre><code>float time_scale = Engine::get_singleton()-&gt;get_time_scale();\nbool exit = false;\nint iters = 0;</code></pre><p><code>time_scale</code>\t全局时间缩放系数</p><p><code>exit</code>\t循环退出标志</p><p><code>iters</code>\t本次渲染帧内执行的物理迭代次数计数器，用于性能监控和调试</p><h4>限制帧数</h4><p></p><p></p><p></p><pre><code>while (time_accum &gt; frame_slice) {</code></pre><p>这是整个机制的核心控制逻辑：</p><p><code>time_accum</code> 是当前积攒的\"时间余额\"（上一轮对话中通过 time_accum += step 注入的）</p><p><code>frame_slice</code> 是固定物理步长（默认 1/60 秒）</p><p>每次迭代末尾执行 <code>time_accum -= frame_slice</code>，消费掉一个步长的时间</p><h4>驱动游戏逻辑</h4><p>物理步进的生产者</p><p></p><p></p><p></p><pre><code>if (OS::get_singleton()-&gt;get_main_loop()-&gt;iteration(frame_slice * time_scale)) {\n    exit = true;\n    break;\n}</code></pre><p>传入 <code>frame_slice * time_scale</code> 作为 <code>delta</code> 参数。</p><p>这个调用会沿着场景树向下传播，最终触发每个节点的 <code>_physics_process(delta)</code> 回调。</p><h4>刷新消息队列</h4><p></p><p></p><p></p><pre><code>message_queue-&gt;flush();</code></pre><p>处理 <code>_physics_process</code> 中通过 <code>call_deferred()</code>、<code>set_deferred()</code>&nbsp;、<code>queue_free</code> 等投递的延迟调用。这些操作在遍历、信号分发等过程中立即执行会破坏数据结构。</p><p>确保在物理步进之前，所有由游戏逻辑产生的状态变更已经生效。</p><h4>执行物理步进</h4><p>_physics_process的消费者</p><p></p><p></p><p></p><pre><code>PhysicsServer3D::get_singleton()-&gt;step(frame_slice * time_scale);\nPhysicsServer2D::get_singleton()-&gt;step(frame_slice * time_scale);</code></pre><p>这是真正的物理模拟推进：</p><p>物理引擎（Godot 3D 默认使用 Jolt Physics，2D 使用内置引擎）以固定步长 frame_slice * time_scale 执行一次完整的物理模拟，包括：</p><ul><li>刚体运动积分（速度、位置更新）</li><li>碰撞检测与碰撞响应</li><li>约束求解（关节、接触点）</li><li>力的施加与阻尼</li></ul><p>传入的 delta 同样是固定值，保证物理模拟的确定性——相同输入永远产生相同输出。</p><p>time_scale 的作用在这里体现：设为 0.5 时，物理引擎认为只过了半个步长的时间，效果就是慢动作。</p><h4>消费 &amp; 再次刷新消息队列</h4><p></p><p></p><p></p><pre><code>time_accum -= frame_slice;\nmessage_queue-&gt;flush();</code></pre><p>time_accum -= frame_slice：从累加器中扣除一个物理步长的时间。这是 while 循环能够终止的关键——每轮迭代消费固定量，直到余额不足一个步长。</p><p>message_queue-&gt;flush()：第二次刷新。物理步进过程中可能又产生了新的延迟调用（如碰撞回调中调用了 call_deferred()），这里再次处理，确保进入下一轮迭代前状态干净。</p><h4>记录物理帧耗时</h4><p></p><p></p><p></p><pre><code>fixed_process_max = MAX(OS::get_singleton()-&gt;get_ticks_usec() - fixed_begin, fixed_process_max);\niters++;</code></pre><p>fixed_begin 是循环开头记录的时间戳，OS::get_singleton()-&gt;get_ticks_usec() - fixed_begin 就是本次物理迭代的实际耗时（微秒）。</p><p>MAX(...) 取最大值：fixed_process_max 记录的是所有物理迭代中最耗时的那一次的耗时。这不是累加，而是取峰值。</p><p>这个值通常用于性能监控和自动帧率调节——如果物理计算太慢，引擎可以据此降低负载或发出警告。</p><p>iters++：计数本次渲染帧内执行了多少次物理迭代。递归构建节点：引擎根据蓝图里记录的层级，从根节点开始，递归地（深度优先） 在内存中 new 出每一个节点（Node）及其子节点。你可以理解为把图纸上的每个房间和零件都实际造出来。</p><p></p><p>设置属性与脚本：引擎为每个新节点设置好蓝图里写的所有属性（比如 position = Vector2(100, 200)），并为节点挂载上对应的脚本（GDScript/C#）实例。</p><p></p><pre><code>重要状态：在这一步结束时，所有节点对象都已经在内存中创建完毕，但它们处于一种 “游离态”（Orphan）。它们互相知道父子关系，但还没有与引擎的主循环（SceneTree）产生任何联系。这意味着它们此时无法处理输入、无法播放动画、_ready() 也还没有被调用。void Node::_propagate_enter_tree(Node *p_root) {\n    // 对 p_root 及其所有后代，执行深度优先的前序遍历\n    // 对每个节点执行以下操作：\n    \n    // ① 设置 SceneTree 指针\n    node-&gt;data.tree = get_tree();\n    \n    // ② 设置 Viewport 指针\n    node-&gt;data.viewport = get_viewport();\n    \n    // ③ 发送 NOTIFICATION_ENTER_TREE 通知\n    node-&gt;notification(NOTIFICATION_ENTER_TREE);\n    //   ↑ 这会触发 Node::_notification() → 调用虚方法 _enter_tree()\n    \n    // ④ 发出 tree_entered 信号\n    node-&gt;emit_signal(\"tree_entered\");\n    \n    // ⑤ 递归处理子节点（前序：先处理当前节点，再处理子节点）\n    for (int i = 0; i &lt; node-&gt;children.size(); i++) {\n        _propagate_enter_tree(node-&gt;children[i]);\n    }\n    \n    // ⑥ 所有子节点都入树后，发送 NOTIFICATION_READY\n    //    （仅当该节点及其所有后代都已入树时）\n    node-&gt;notification(NOTIFICATION_READY);\n    //   ↑ 触发虚方法 _ready()\n    node-&gt;emit_signal(\"ready\");\n}</code></pre><table><tbody><tr><th>头1</th><th>头2</th><th>头3</th></tr><tr><td>乙</td><td>丙丁<br></td><td><br></td></tr><tr><td><br></td><td><br></td><td><br></td></tr><tr><td><br></td><td><br></td><td><br></td></tr><tr><td><br></td><td><br></td><td><br></td></tr></tbody></table><p><br></p>",
  "createdAt": "2026-09-08T12:40:43.769Z",
  "updatedAt": "2026-09-15T09:28:47.153Z"
}